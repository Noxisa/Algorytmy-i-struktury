/*
Tomas Chmelevski. Student
* -----------------
*Sortowanie algorytmem MergeSort. Implementacja rekurencja. Blokowy schemat
* -----------------
2022-12-06
*/
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int   mergeSort(int arr,int left,int right,int end){
    if (left > right)
        return right;
  }
int right, left;
   int mid = (left+right)/2;
    int  mergeSort(int arr,int left,int  mid, int end);
    int MergeSort(int arr,int  mid+1,int  right);
   int  merge(int arr, int left, int  mid, int  right);
return end;
int main(){ 
  int  mergeSort()
}
